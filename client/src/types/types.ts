export type uuid = `${string}-${string}-${string}-${string}-${string}`

export type BookType = {
  uuid: uuid;
  title: string;
  genre: string;
  author: string;
  pages: string;
  words: string;
}


