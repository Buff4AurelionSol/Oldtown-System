import { initialBooks } from "../../../../consts/const"

export const INITIAL_CONTEXT_BOOK = {
  books: initialBooks,
  setBooks: () => null,
  addBook: () => null,
  cleanBooks: () => null,
  deleteBook: () => null

}